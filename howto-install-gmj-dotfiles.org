Notes to self on downloading, changing and updating my dotfiles on github

#+BEGIN_EXAMPLE
# $ git clone https://github.com/eludom/dotfiles.git
[make some chages, e.g. add a new file]
$ git add 30-blogging.org 
$ git commit -mnew
$ git add 30-blogging.org 
$ git commit -mnew
#+END_EXAMPLE
